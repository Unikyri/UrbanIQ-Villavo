<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme compatible - Modo Oscuro -->
    <style name="Theme.Pasajero" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/urban_light_blue</item>
        <item name="colorPrimaryDark">@color/urban_blue_variant</item>
        <item name="colorAccent">@color/urban_green</item>
        <item name="android:statusBarColor">@color/urban_blue_variant</item>
        <item name="android:windowBackground">@color/dark_background</item>
    </style>
</resources>